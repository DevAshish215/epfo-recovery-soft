# Server Configuration
PORT=5000

# MongoDB Configuration
# Do not commit .env file to version control
# Use cloud Mongo URI during deployment (e.g., MongoDB Atlas)
MONGO_URI=mongodb://localhost:27017/epfo_recovery

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Groq API (for AI letter generation) - get free key at https://console.groq.com
GROQ_API_KEY=
